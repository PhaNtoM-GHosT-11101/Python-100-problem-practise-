"""
Problem 74: yield from and Nested Generators
---------------------------------------------
Use 'yield from' to:
1. Flatten a deeply nested list of arbitrary depth
2. Create a tree traversal generator (pre-order)

nested = [1, [2, [3, 4], 5], [6, 7]]
flat = list(flatten(nested))  → [1, 2, 3, 4, 5, 6, 7]
"""

# Write your solution below:


